# Two Sum

Problem: Given an array of integers and a target, return indices of two numbers that add up to target.

Approach: Hash table for complement lookup — O(n) time, O(n) space.

Sample:

Input: nums = [2,7,11,15], target = 9
Output: [0,1]
